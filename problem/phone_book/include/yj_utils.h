#ifndef YJ_UTILS_H
#define YJ_UTILS_H

/**
 * @brief 문자열 비교 함수
 * 
 * @param a 
 * @param b 
 * @return 0(성공), 다른 값 (실패 )
 */
int yj_cmp_str(char *a, char *b);

/**
 * @brief 인트로 print 함수
 * 
 */
void print_info();
#else
#endif